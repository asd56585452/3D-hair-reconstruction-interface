#version 330 core

layout (location=0) in vec3 vertexPos;
layout (location=1) in vec3 normal;

uniform mat4 model;
out vec4 nc;

void main()
{
    gl_Position = model * vec4(vertexPos, 1.0);
    nc = model * vec4(normal, 0.0);
}
